# Production Requirements
# Database
psycopg2-binary==2.9.12
sqlalchemy==2.0.23
alembic==1.12.1

# API & Web
fastapi==0.136.3
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0

# Caching & Sessions
# Redis saiu junto com o crm_api.py — nenhum módulo do sistema o usa hoje.

# Security & Auth
cryptography==41.0.7
pyjwt==2.8.0
bcrypt==4.2.1
passlib[bcrypt]==1.7.4
email-validator==2.1.0

# Observabilidade: as dependências foram removidas junto com o crm_api.py, o
# único serviço que exportava métricas e usava log estruturado. Se a
# instrumentação voltar, reinstale prometheus-client e python-json-logger.

# Development/Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
black==23.12.0
ruff==0.1.8

# AWS (optional, for cloud deployment)
# Precisa ser >= 1.35 para o botocore aceitar urllib3>=2.7.0 (piso de
# segurança do requirements.txt, PYSEC-2026-141/142). A versão antiga
# (1.33.12) prendia o botocore a urllib3<2.1, o que quebra a resolução
# assim que os dois arquivos são instalados juntos.
boto3==1.43.82

# Data & Utilities
# Precisa ficar >= 1.2.2 (PYSEC-2026-2270, o mesmo piso de requirements.txt).
# Um pin exato abaixo disso faz o pip falhar a resolução assim que os dois
# arquivos são instalados juntos (web_requirements.txt referencia os dois).
python-dotenv==1.2.3
pydantic>=2.9.0,<3
pydantic-settings>=2.6.0
